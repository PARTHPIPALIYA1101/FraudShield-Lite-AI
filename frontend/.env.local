# FraudShield frontend — local dev config.
# NEXT_PUBLIC_* vars are inlined into the client bundle (safe: just service URLs).
# Match the backend host/port from docker-compose + uvicorn (default :8000).
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_WS_URL=/api